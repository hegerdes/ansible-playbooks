# Add users to docker group
---
- name: Create users are added to the docker group.
  user:
    name: '{{ item }}'
    groups: [docker]
    append: true
    state: present
  with_items: '{{ docker_users }}'
