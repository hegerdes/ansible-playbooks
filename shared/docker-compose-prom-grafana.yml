services:
  prometheus:
    image: prom/prometheus:latest
    command: --config.file=/etc/prometheus/prometheus.yml
    container_name: mon-prometheus
    restart: unless-stopped
    hostname: prometheus
    volumes:
      - /srv/cri-bench/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus

  grafana:
    image: grafana/grafana:latest
    container_name: mon-grafana
    hostname: grafana
    environment: {}
    ports:
      - 3000:3000
    volumes:
      - grafana-data:/var/lib/grafana

volumes:
  grafana-data:
  prometheus-data:

# global:
#   scrape_interval: 5s

# scrape_configs:
#   - job_name: "cri-bench-host"
#     honor_labels: true
#     metrics_path: /metrics
#     static_configs:
#       - targets:
#           - "10.0.0.3:9100"

#   - job_name: "cri-bench-containerd"
#     honor_labels: true
#     metrics_path: /v1/metrics
#     static_configs:
#       - targets:
#           - "10.0.0.3:1234"
